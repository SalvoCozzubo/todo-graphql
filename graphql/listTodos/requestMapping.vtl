#set($page=$ctx.args.page)
#set($offset = 0)
#if($offset != $null)
  #set($offset = $ctx.args.page)
#end
#set($limit=10)
#set($offset=$offset*$limit)
{
  "version": "2018-05-29",
  "statements": [
    "SELECT COUNT(ID) as num FROM `todo`",
    "SELECT * FROM `todo` LIMIT :OFFSET , :LIMIT"
  ],
  "variableMap": {
    ":OFFSET": $offset,
    ":LIMIT": $limit
  }
}